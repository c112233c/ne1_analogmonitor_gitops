apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: analogmonitor-fe-30087
  namespace: boat

spec:
  refreshInterval: 1h

  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore

  target:
    name: analogmonitor-fe-30087-secret
    creationPolicy: Owner

  dataFrom:
  - extract:
      key: apps/analogmonitor-fe-30087
